# Capture the set of staged files before formatting
STAGED_FILES=$(git diff --cached --name-only)
bun run fmt
# Re-stage only those files
echo "$STAGED_FILES" | xargs git add
bun run lint
bun run check-types